import { Typography, Box } from '@mui/material'

const LoginPage = () => {
  return (
    <Box sx={{ textAlign: 'center', py: 4 }}>
      <Typography variant="h4">Login Page</Typography>
      <Typography>Login functionality coming soon</Typography>
    </Box>
  )
}

export default LoginPage